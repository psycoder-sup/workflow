{
  "name": "workflow",
  "version": "0.5.0",
  "description": "Spec-driven delivery pipeline: /spec (PRD) -> /taskplan (partitioned plan) -> /implement (parallel orchestrated build; /implement-orca for Orca-dispatched workers) -> /cleanup (PR, CI, merge, teardown), plus /project-kit project scaffolding.",
  "author": {
    "name": "SangUk Park"
  },
  "keywords": [
    "workflow",
    "orchestration",
    "spec",
    "planning",
    "subagents",
    "claude-code"
  ]
}
